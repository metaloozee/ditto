# Implementation Plans

Generated on 2026-06-24. Updated on 2026-07-02 with plans 018–020 (the three-phase Pi-SDK rewrite from `docs/pi-sdk-session-broker-prd.md`) added against commit `55b6151`, and updated again on 2026-07-02 with deep-audit follow-up plans 021–024 plus `plans/deferred-findings-2026-07-02.md` against commit `bb00b96`. Plans 015-017 were reconciled on 2026-07-01 against `e3d5209`. Updated again on 2026-07-02 for `docs/four-layer-flue-workflow-rewrite-prd.md`: plans 021–024 are rejected for future execution because they continue the superseded Pi runner path, and plan 025 is the new Phase 0 Flue/TanStack/Alchemy/Sandbox integration proof. Updated again on 2026-07-02 with Phase 1 plans 026–029 (Flue build path, D1 metadata/projection schema, coordinator SQLite storage, and R2 snapshot/artifact layout) against commit `38de789`. Updated again on 2026-07-03 with Phase 2 plans 030–034 (Flue event projection, dispatch adapter, read-only agent, stream bridge DO, and startRun wiring) against commit `e915ff1`. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Implement the GitHub App Auth Flow | P1 | M | — | BLOCKED (stale: partial independent implementation changed plan assumptions) |
| 002 | Database Schema and tRPC Projects | P1 | S | — | DONE |
| 003 | Sandbox Provisioning and Bootstrap | P1 | M | 002 | BLOCKED (local `pnpm dev` container support unavailable on Windows; use WSL for final dev verification) |
| 004 | Restore GitHub App helper and branch-list typecheck | P1 | S | — | TODO |
| 005 | Remove dead project-route UI code and restore verification | P1 | S | — | TODO |
| 006 | Restore Yarn lockfile handling in sandbox bootstrap | P1 | S | — | TODO |
| 007 | Normalize escaped GitHub App private-key newlines | P2 | S | 004 | REJECTED (intentional behavior: do not normalize escaped key newlines) |
| 008 | Add project-scoped agent run foundation | P1 | L | 003 | DONE |
| 009 | Scope workspace events to the selected session | P1 | S | 008 | DONE |
| 010 | Replace startRun's D1 transaction with batched writes | P1 | M | 008, 009 | DONE |
| 011 | Create GitHub import regression tests | P1 | M | — | DONE |
| 012 | Paginate GitHub import state and branch discovery | P1 | S | 011 | DONE |
| 013 | Authorize GitHub installation use server-side | P1 | M | 011, 012 | DONE |
| 014 | Validate sandbox env-var keys before saving or provisioning | P2 | S | — | DONE |
| 015 | Persist and restore project sandboxes | P1 | L | — | DONE |
| 016 | Add the Flue project-coder foundation | P1 | L | 013, 014, 015 | REJECTED (superseded by direct Pi Durable Object broker plan 017) |
| 017 | Add the Pi session-broker foundation | P1 | L | 013, 014, 015 | DONE |
| 018 | Build the runner↔broker NDJSON contract module, tests, and the Pi SDK agent runner | P1 | L | 017 | DONE |
| 019 | Rewire the WorkspaceSessionBroker DO to launch the Pi SDK runner | P1 | L | 018 | DONE |
| 020 | Harden the runner↔broker path (redaction, ready timeout, stale-runner cleanup) | P2 | M | 018, 019 | DONE |
| 021 | Add runner verification and reproducible installs | P1 | M | 018, 019, 020 | REJECTED (superseded by four-layer Flue rewrite PRD; do not execute) |
| 022 | Gate runner events and commands by run id | P1 | M | 021 | REJECTED (superseded by four-layer Flue rewrite PRD; do not execute) |
| 023 | Restart the runner when the selected model changes | P1 | S | 022 | REJECTED (superseded by four-layer Flue rewrite PRD; do not execute) |
| 024 | Persist and replay active-run live stream state | P1 | M | 022, 023 | REJECTED (superseded by four-layer Flue rewrite PRD; do not execute) |
| 025 | Prove the Four-Layer Flue Integration Boundary | P1 | L | — | DONE |
| 026 | Stabilize the Flue/Alchemy Build and Local Dev Path | P1 | M | 025 | DONE |
| 027 | Establish D1 Metadata and Projection Schema for the Four-Layer Model | P1 | M | 025 | DONE |
| 028 | Move ProjectCoordinator State to SQLite Tables and Add Status APIs | P1 | M | 025 | DONE |
| 029 | R2 Snapshot and Artifact Key Layout and Manifest Helpers | P1 | M | 025 | DONE |
| 030 | Flue Event Projection Contract | P1 | S | — | TODO |
| 031 | Flue Dispatch and Stream Adapter | P1 | M | 030 | TODO |
| 032 | Real Flue Project-Coder Agent with Read-Only Tools | P1 | M | — | TODO |
| 033 | Flue Run Stream Bridge Durable Object | P1 | L | 030, 031 | TODO |
| 034 | Wire Read-Only Flue Dispatch into startRun | P1 | M | 031, 032, 033 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale — finding fixed independently or approach abandoned)

## Dependency notes

- 002 no longer depends on 001. The current codebase already exposes repository import state and installation ids, so this plan can land independently as the persistence layer.
- 003 depends on 002 because sandbox bootstrap updates `projects` records with `sandboxId` and readiness state.
- 004 and 005 should land first because they restore typecheck/lint/whitespace verification for the uncommitted branch.
- 007 was rejected on 2026-06-25 because the maintainer confirmed no private-key normalization should be added.
- 008 depends on the project sandbox bootstrap surface from 003: projects must already have `sandboxId` and `status` before the workspace/run lock can be useful. It intentionally chooses one sandbox per project, with logical sessions and one active mutating run lock inside that project sandbox.
- 009 depends on 008 because it fixes the workspace event API introduced by the agent-run foundation: conversation events must be scoped by selected session, while the mutating-run lock remains project-scoped.
- 010 depends on the workspace/run model from 008 and the current session-scoped workspace behavior from 009. It is a focused reliability fix for `workspace.startRun`: remove unsupported D1 `db.transaction(...)`, preserve the project-level mutating-run lock, and use D1-compatible batched writes for initial session/run/event rows.
- 011 adds the missing regression harness around GitHub import state before more behavioral fixes land in the same area.
- 012 depends on 011 because the pagination fix should extend the shared GitHub import tests instead of inventing a second harness.
- 013 depends on both 011 and 012 because the authorization check must be tested and must validate against the full paginated import-state result, not page 1 only.
- 014 is independent of 011-013, but it stays later in the queue because malformed env-var keys are lower-severity than the GitHub import authorization and pagination bugs. It now covers both GitHub import env-var entry and later project-settings env-var saves.
- 015 has no prior plan dependency because the current source already contains the sandbox bootstrap surface it needs. It should still be reviewed as an extension of the v1 one-sandbox-per-project decision from 003 and the workspace/run model from 008-010. It is the durability layer for ready projects whose sandbox container or `/workspace` contents disappear.
- 016 depended on 013/014/015 for the same reasons 017 now does, but it is rejected because the Flue worker composition path was abandoned.
- 017 depends on 013 and 014 because the real Pi runner still must not trust client-submitted GitHub repo/install pairs and must not rely on malformed env-var keys being safe to sync into `/workspace/.env`. It depends on 015 because the runner still uses the same long-lived project sandbox and must preserve the pre-execution `ensureProjectSandbox` path plus post-success backup refresh.
- 018 is phase 1 of the Pi-SDK rewrite in `docs/pi-sdk-session-broker-prd.md`. It depends on 017 only loosely: it extends the Dockerfile that 017 created (Pi is already globally installed in the image) and runs alongside the existing CLI-RPC DO without modifying it. It adds the pure `src/lib/runner-protocol.ts` contract module + tests (the single automated seam the maintainer agreed to) and the `sandbox/runner/` Node.js program that consumes the Pi SDK. It does NOT rewire the DO, tRPC, browser, or D1.
- 019 is phase 2 of the Pi-SDK rewrite. It depends on 018 being DONE: it rewires `WorkspaceSessionBroker` to launch the runner via `startProcess` (stdin from a FIFO) and consume `RunnerEvent`s instead of Pi RPC events, while keeping `WorkspaceSessionBrokerFrame`, the tRPC API, and the D1 schema shape-stable. It deletes `pi-rpc.ts`, `pi-rpc.test.ts`, and `sandbox/pi/ditto-ask-user.ts` (dead after the rewire) and removes the `COPY sandbox/pi/` line from the Dockerfile. The DO still does the `git status` workspace inspect after `tool_finished` (moving that into the runner is a deferred optimization, not Pi wire protocol).
- 020 is phase 3 (hardening) of the Pi-SDK rewrite. It depends on 018 and 019 being DONE. It adds a pure `redactSecrets` helper + tests to the contract module (the one new automated seam), uses it in the runner before emitting `tool_progress`/`error`, bounds the runner `ready` wait at 30s in the DO, adds stale-runner liveness checks + stale-lock cleanup in `ensureRunnerProcess`, and settles the PRD's open restart-continuity question as "start fresh, no D1 replay" (documented in the runner + the PRD).
- 021–024 are rejected for future execution because `docs/four-layer-flue-workflow-rewrite-prd.md` supersedes the Pi runner/broker path where it conflicts. Keep the files as historical context only; do not ask an executor to continue that sequence.
- 025 has no code dependency on 021–024. It is the new Phase 0 proof required before writing implementation plans for the four-layer Flue rewrite.
- 026 depends on 025 (DONE). It stabilizes the Flue build path that Phase 0 left manual: adds a `flue:build` script wired into `dev`/`build`/`deploy`, ignores generated `.flue-vite*` artifacts, names the `flueWorker` entrypoint constant, and documents the local dev/deploy sequence. It does not change product behavior, D1, the coordinator, or Flue agent definitions.
- 027 depends on 025 (DONE). It extends `src/db/schema.ts` with additive, nullable four-layer columns on `agent_runs` (`flueAgentName`, `flueAgentInstanceId`, `flueSubmissionId`, `flueStreamOffset`, `snapshotId`, `errorCode`) and `projects` (`lockStatus`, `lockHolderRunId`, `lockFencingToken`, `lockUpdatedAt`), adds `snapshots` and `run_artifacts` tables, generates migration `0005_*.sql`, and adds a pure tested `project-run-projection.ts` helper. No code writes to the new columns yet; later phases populate them.
- 028 depends on 025 (DONE). It decomposes the coordinator's single-key JSON blob into four DO-local SQLite tables (`coordinator_meta`, `mutation_lease`, `read_only_runs`, `last_terminal`) via `this.ctx.storage.sql`, keeps the pure admission/terminal decision functions and their tests byte-for-byte unchanged, and keeps the HTTP routes (`GET /status`, `POST /admit`, `POST /terminal`) shape-stable. No queueing, lease expiry, or WebSocket fan-out (Phase 3).
- 029 depends on 025 (DONE). It adds pure, tested R2 key/manifest helpers (`snapshotManifestKey`, `snapshotArchiveKey`, `artifactKey`, `buildSnapshotManifest`, `validateSnapshotManifest`, `resolveSnapshotPointer`) that establish the project-scoped prefix layout, secret-exclusion manifest, and the "D1 pointer updates only after R2 write succeeds" policy. No live R2 writes (Phase 4).
- 026, 027, 028, and 029 touch disjoint files and have no hard code dependency on each other (all depend only on 025). Recommended execution order is 026 → 027 → 028/029: 026 first so the Flue build path is stable when later plans run the verification gate, 027 before 029 so the `snapshots`/`run_artifacts` D1 tables exist before Phase 4 wires the manifest helpers to real R2 writes. 028 and 029 may execute in either order or in parallel.
- 030 is the Phase 2 foundation: a pure `mapFlueEventToDittoEvents` function that maps Flue runtime events (`text_delta`, `tool_start`, `tool`, `idle`, `agent_end`, `log`) to Ditto's `agentRunEvents` vocabulary. It has no dependencies and no imports from `@flue/runtime` — it stays pure by accepting a structural `FlueEventInput` type. Plan 031 and 033 both call it.
- 031 depends on 030 (DONE). It creates `createFlueDispatchAdapter` with injectable fetch functions, plus `createServiceBindingDispatchFetch` and `createServiceBindingStreamFetch` factories that wire to `env.FLUE_WORKER.fetch`. The adapter is fully testable with fake fetch functions — no live Flue Worker, LLM, or sandbox needed. Plan 033 calls the service-binding factories to wire the real dispatch path.
- 032 is independent of 030/031 — it only touches `.flue/agents/project-coder.ts` and `package.json` (adds `valibot`). It rewrites the spike agent into a real read-only coding agent with 5 tools (`read_file`, `list_directory`, `git_status`, `git_diff`, `run_readonly_command`), path-traversal guards, and real instructions. It can execute in parallel with 030/031. The `pnpm flue:build` gate verifies the agent compiles against the installed `@flue/runtime` API.
- 033 depends on 030 and 031 (both DONE). It creates the `FlueRunBridge` Durable Object: a per-session DO that dispatches to the Flue Worker via the `FLUE_WORKER` service binding, consumes the Flue Durable Stream, projects events to D1 via `mapFlueEventToDittoEvents`, broadcasts to the browser via WebSocket (same frame vocabulary as `WorkspaceSessionBroker`), and calls `ProjectCoordinator /terminal` on terminal events. It also declares the DO in `alchemy.run.ts` (migration tag `v4`) and exports it from `src/server.ts`.
- 034 depends on 031, 032, and 033 (all DONE). It is the integration slice: branches `workspace.startRun` so read-only runs (`isMutating: false`) go through coordinator admission → `FlueRunBridge /start`, while mutating runs keep the existing Pi runner path. It also updates the socket route to proxy to `FlueRunBridge` when the session's most recent run has `flueAgentName` set. The `startRun` input schema and response shape are unchanged — no UI changes.
- 030, 031, and 032 are the recommended first three to execute: 030 → 031 (sequential, since 031 imports from 030), with 032 in parallel. Then 033 (depends on 030+031), then 034 (depends on 031+032+033). The full Phase 2 execution order is: 030 → 031 → 033 → 034, with 032 executable at any point before 034.
- Findings 6–11 from the 2026-07-02 deep audit were deferred into `plans/deferred-findings-2026-07-02.md`; they are not executable plans yet.

## Verification baseline

Use these commands when executing plans:

- `pnpm exec tsc --noEmit` — passes typechecking.
- `pnpm lint` — exits 0 at plan-writing time with existing warnings in `src/components/ui/grainient.tsx:297` and `src/components/ui/sidebar.tsx:85`; new plans should not add warnings in touched files.
- `pnpm test` — exits 0 with 10 test files and 73 tests passing at Phase 2 plan-writing time (commit `e915ff1`, after plans 026-028 merged to master). Plans 030 and 031 each add one test file and increment the count. Plan 033 adds one test file. Plan 034 adds no new tests.
- `pnpm flue:build` — exits 0 with the known generated-wrangler migration warning only. Required by plans 032 and any plan that changes `.flue/` source.
- `git diff --check` — catches whitespace errors before commit.
- Runner-specific `pnpm runner:typecheck` belonged to the rejected Pi runner sequence in plans 021–024; do not treat it as required for plan 025 unless a future plan reintroduces a sandbox runner.

## Reconciliation notes

- 2026-06-29: plans 011-014 drifted from their original `93db448` assumptions because tRPC procedures moved out of `src/integrations/trpc/router.ts` into split router modules. Refreshed all four plans in place at `8632f47`; none were rejected because the underlying findings still exist.
- 2026-06-29: plan 011 still establishes the first GitHub import regression tests; current `pnpm test` passes with no test files.
- 2026-06-29: plan 011 was reconciled again for external OpenCode/pi.dev execution. Current HEAD is still `8632f47`, its drift check is clean, and the expected GitHub import test files are still absent, so it remains executable as TODO.
- 2026-06-29: plans 012 and 013 now target `src/integrations/trpc/routers/github.ts` and `src/integrations/trpc/routers/projects.ts`; 012 still depends on 011, and 013 still depends on 011 plus 012.
- 2026-06-29: plan 014 now includes `src/components/project-settings-dialog.tsx` and `projects.setEnvVar`, while explicitly preserving deletion of legacy malformed env-var keys.
- 2026-06-30: added plan 016 from `docs/flue-agent-harness-prd.md`. It records the maintainer's decisions to use direct Flue client streaming, OpenCode Go selectable models, Zustand user preferences, full sandbox access after same-worker mount proof, lock checks before edits, future commit-only export, and D1 metadata plus R2 artifacts for oversized outputs.
- 2026-06-30: plan 013 was reconciled to commit `e632ba0`, executed in isolated worktree `/tmp/opencode/ditto-plan-013`, and reviewed as DONE on branch `advisor/013-github-authz`.
- 2026-06-30: plan 014 was refreshed to commit `de51de1` after plan 013 added GitHub repository authorization in `src/integrations/trpc/routers/projects.ts`; the env-var key validation finding and required behavior are unchanged.
- 2026-06-30: plan 014 was executed in isolated worktree `/tmp/opencode/ditto-plan-014`, reviewed as DONE on branch `advisor/014-validate-env-var-keys`, with commits `91f46b0` and `7996275`.
- 2026-07-01: reconciled plans 015 and 016 against commit `9e2fed0`. Plan 015 drifted because plan 014 changed env-var validation in `src/integrations/trpc/routers/projects.ts`, but the weak sandbox readiness and direct `.env` sync findings still exist. Plan 016 drifted only in planning docs/index, but its dependency graph was tightened: Step 1 may run first as a Flue mount spike; the full Flue runner now depends on Plan 015 and must use its ensure/restore and backup-refresh helpers.
- 2026-07-01: plan 015 was executed in isolated worktree `/tmp/opencode/ditto-plan-015`, reviewed as DONE on branch `advisor/015-durable-sandbox-restore`, with commits `a3563a2`, `9bdab28`, `3dc48bc`, `4269328`, and `fcd6e17`. Reviewer requested the final R2 DX decision: backup credentials are required at app startup, so local omission is not supported.
- 2026-07-01: plan 016 was refreshed to commit `e3d5209` after plan 015 landed. Its source excerpts now reflect the Plan 015 `ensureProjectSandbox` path, backup helper exports, R2 backup bindings, and expanded baseline tests.
- 2026-07-01: plan 016 execution stopped in isolated worktree `/tmp/opencode/ditto-plan-016` during Step 1. `npx flue build --target cloudflare` works for the minimal Flue app and generates `FlueProjectCoderAgent`/`FlueRegistry`, but the plan does not yet specify how to compose Flue's generated Cloudflare Worker entrypoint and DO migrations with Alchemy/TanStack's current `src/server.ts` entrypoint and Sandbox binding.
- 2026-07-01: after research into Flue, Pi, Durable Objects, and the installed Sandbox SDK, the Flue approach was abandoned. New plan 017 replaces it with a same-Worker direct Pi architecture: one authenticated DO per `workspace_sessions.id`, live browser events over a DO WebSocket, Pi RPC mode inside the existing project sandbox, and D1 as the canonical persisted event log.
- 2026-07-01: plan 017 was executed in isolated worktree `/tmp/opencode/ditto-plan-017`, reviewed as DONE on branch `advisor/017-pi-session-broker-foundation`, with commits `b00ffc7`, `4bf1eb0`, `88d31a1`, and `706cf50`. Reviewer requested two revision rounds for Step 3 type-instantiation failure and run-state hardening. Final command verification passed: `pnpm exec tsc --noEmit --pretty false`, `pnpm lint`, `pnpm test`, and `git diff --check`. Authenticated browser smoke flows were not run in this session; run them before deploying.
- 2026-07-02: added plan 018 from `docs/pi-sdk-session-broker-prd.md` (phase 1 of the Pi-SDK rewrite). Recon confirmed the installed `@cloudflare/sandbox@0.12.1` (latest npm `0.12.3`) exposes no `stdin`/`execInteractive` API, so the PRD's open transport decision is resolved in the plan as the named-pipe fallback (the runner reads NDJSON from `process.stdin`, transport-agnostic). Pi SDK API names (`createAgentSession`, `defineTool`, `session.subscribe`, event shapes) were verified against current Pi docs via Context7. Plan 018 is purely additive: it does not touch the DO, tRPC, browser, D1, or alchemy bindings.
- 2026-07-02: added plans 019 and 020, completing the Pi-SDK rewrite's three-phase sequencing (PRD line 207). Plan 019 (phase 2) rewires `WorkspaceSessionBroker` to launch the runner 018 built and consume `RunnerEvent`s, deletes `pi-rpc.ts`/`pi-rpc.test.ts`/`ditto-ask-user.ts`, and keeps `WorkspaceSessionBrokerFrame` + the tRPC API + the D1 schema shape-stable (verified against live `use-workspace-session-socket.ts`, `ai-chat.tsx`, `composer.tsx`, and `project.$projectId.tsx`). Plan 020 (phase 3) adds a pure `redactSecrets` seam + tests, bounds the runner `ready` wait at 30s, adds stale-runner liveness checks + stale-lock cleanup, and settles the PRD's open restart-continuity question as "start fresh, no D1 replay." Both plans stamp commit `55b6151`; execution order is 018 → 019 → 020, strictly sequential.
- 2026-07-02: plan 018 was executed in isolated worktree `/tmp/opencode/ditto-plan-018`, reviewed as DONE on branch `advisor/018-runner-contract-and-pi-sdk-runner`, commit `d4edddd`. All verification gates pass: `tsc --noEmit`=0, `pnpm test`=0 (45 pass, 16 new in `runner-protocol.test.ts`), `pnpm lint`=0 (only the 2 pre-existing warnings), `git diff --check`=0. Two documented deviations approved: (1) `sandbox/runner/package.json` lists `@earendil-works/pi-ai@^0.80.3` instead of `typebox` — official Pi examples import `Type` from `pi-ai`; (2) `sandbox/runner/tsconfig.json` adds `types:["node"]` — the runner is a Node program, not a Worker, so inherited Worker `types` don't resolve. The hardening STOP condition was resolved via a custom `ResourceLoader` (official `12-full-control` pattern) returning empty extensions/skills/prompts/themes — the SDK equivalent of `--no-extensions`/`--no-skills`/`--no-prompt-templates`/`--no-themes`; AGENTS.md context files are intentionally disabled for the spike (phase 3 may re-enable a constrained walk). The optional in-repo runner typecheck passed against the real Pi SDK types. The manual runner smoke (Step 6) was NOT run — it requires a running sandbox + `OPENCODE_API_KEY` and is non-gating per the plan; the stdout-clean-NDJSON guarantee is therefore not yet empirically verified end-to-end. Run the smoke before phase 2 (plan 019) depends on the runner.
- 2026-07-02: plan 019 was executed in isolated worktree `/tmp/opencode/ditto-plan-019`, reviewed as DONE on branch `advisor/019-broker-launches-pi-sdk-runner`, commit `bd4e7cb`. All verification gates pass: `tsc --noEmit`=0, `pnpm test`=0 (39 tests, 4 files; `pi-rpc.test.ts` deleted, `runner-protocol.test.ts` 16 tests still green), `pnpm lint`=0 (only the 2 pre-existing warnings), `git diff --check`=0. The DO now launches `tsx /opt/ditto/sandbox/runner/index.ts` via `startProcess` with stdin from a FIFO, consumes `RunnerEvent`s via `RunnerEventBuffer`, sends `RunnerCommand`s via `serializeRunnerCommand` + FIFO write, and awaits a `ready` event before sending `prompt`. `WorkspaceSessionBrokerFrame` is byte-for-byte unchanged; `responseWaiters`/`rejectResponseWaiters` fully removed; `BrokerState` uses `runnerProcessId`/`pendingInputRequestId`. `src/lib/pi-rpc.ts`, `src/lib/pi-rpc.test.ts`, and `sandbox/pi/ditto-ask-user.ts` are deleted; the Dockerfile no longer copies `sandbox/pi/`. Four documented deviations approved by the reviewer: (1) `parseRunnerEvent` not directly imported — `RunnerEventBuffer.push()` calls it internally, so a direct unused import would fail `noUnusedLocals`; (2) `writePromise.catch(() => undefined)` queue-resilience pattern fixes a latent queue-poisoning bug while preserving serialization; (3) 4 stale `pi-rpc`/`extension_ui` comment matches remain in plan-018's out-of-scope files (`runner-protocol.ts`, `runner/index.ts`) — correctly left for a plan-018 revision, zero live references; (4) commit amended once for the lefthook/biome formatter, no semantic changes. The `ready` wait has no timeout and the manual browser smoke was not run — both explicitly deferred to plan 020 / pre-deploy per the plan. Run the browser smoke before deploying.
- 2026-07-02: plan 020 was executed in isolated worktree `/tmp/opencode/ditto-plan-020`, reviewed as DONE on branch `advisor/020-harden-runner-broker-path`, commits `a0e36d2` and `9b160e0`. All automated verification gates pass: direct root `tsc --noEmit --pretty false`=0, `pnpm test`=0 (48 tests, including 9 new `redactSecrets` cases), `pnpm lint`=0 (only the 2 pre-existing warnings), and `git diff --check`=0. Scope is clean: only `docs/pi-sdk-session-broker-prd.md`, `sandbox/runner/index.ts`, `src/lib/runner-protocol.test.ts`, `src/lib/runner-protocol.ts`, and `src/lib/workspace-session-broker.ts` changed. The runner now redacts concrete env secrets plus GitHub/PEM/AWS/`sk-` patterns before emitting `tool_progress` or `error`; the broker bounds `ready` waits at 30s and checks `sandbox.listProcesses()` before reusing a stored runner; stale-runner cleanup fails stranded active runs and clears runner state; restart continuity is documented as fresh `AgentSession`, no D1 replay. Manual browser/sandbox smoke was NOT run because `OPENCODE_API_KEY` was unavailable in the executor shell; run it before deploying.
- 2026-07-02: deep audit follow-up added plans 021–024 against commit `bb00b96`: runner verification/reproducible installs, run-id gating for runner events and commands, model-aware runner restart, and active-run live stream replay. Finding 5 (scratch flow no-op) was intentionally not planned because the maintainer chose to disable that surface directly. Findings 6–11 were captured in `plans/deferred-findings-2026-07-02.md` for later planning.
- 2026-07-02: `docs/four-layer-flue-workflow-rewrite-prd.md` became the draft source of truth and explicitly supersedes earlier runner / broker / Flue harness PRDs and plans where they conflict. Plans 021–024 were rejected because they improve the now-superseded Pi runner path. Added plan 025 as the required Phase 0 architecture spike: prove Flue Cloudflare target, TanStack Start, Alchemy, Cloudflare Sandbox, and a project-scoped coordinator can coexist before any broad rewrite.
- 2026-07-02: plan 025 was executed in isolated worktree `/tmp/opencode/ditto-plan-025`, reviewed as DONE on branch `advisor/025-four-layer-flue-spike`, commits `2bdc690` and `6aca616`. All automated verification gates pass in the worktree: `npx flue build --target cloudflare`=0, `pnpm exec tsc --noEmit --pretty false`=0, `pnpm lint`=0 with only the two pre-existing warnings, `pnpm test`=0 (8 files, 62 tests), and `git diff --check`=0. The spike proves a split Worker topology: public `website` Worker plus private Alchemy `flueWorker` service binding, Flue DO namespaces `FlueProjectCoderAgent`/`FlueRegistry`, and a project-scoped `ProjectCoordinator` DO. Reviewer caveat: Flue's generated wrangler file still warns about migrations because Alchemy, not the generated wrangler file, is the intended source of truth for those Durable Object namespaces.
- 2026-07-02: added Phase 1 plans 026–029 against commit `38de789`, all depending on the DONE plan 025. Plan 026 stabilizes the Flue/Alchemy build and local dev path (`flue:build` script, generated-artifact ignores, entrypoint constant, docs). Plan 027 establishes the D1 metadata/projection schema (additive Flue/snapshot/lock columns, `snapshots`/`run_artifacts` tables, migration `0005`, pure `project-run-projection` helper). Plan 028 moves `ProjectCoordinator` state from a single-key JSON blob to four DO-local SQLite tables via `this.ctx.storage.sql`, keeping the pure decision functions and HTTP routes unchanged. Plan 029 adds pure R2 key/manifest helpers (`snapshotManifestKey`/`artifactKey`/`buildSnapshotManifest`/`validateSnapshotManifest`/`resolveSnapshotPointer`) that establish the project-scoped prefix layout, secret-exclusion manifest, and the D1-pointer-after-R2-write policy. All four plans are executor-ready, TDD, and touch disjoint files; recommended order is 026 → 027 → 028/029. No source files were changed by this planning pass — only `plans/` markdown.
- 2026-07-03: plan 026 execution in `/tmp/opencode/ditto-plan-026` stopped at the planned STOP condition. `pnpm flue:build` generated `dist/ditto_plan_026/index.js` in that worktree instead of the plan's `dist/ditto_plan_025/index.js`, proving Flue's default Worker/output name depends on the root basename. A follow-up isolated probe confirmed no committed `wrangler.jsonc` is needed: deriving the Alchemy entrypoint from `path.basename(process.cwd()).replaceAll("-", "_")` produced `dist/ditto_plan_026/index.js` in the worktree, `pnpm flue:build` exited 0 with only the known Durable Object migration warning, and `pnpm exec tsc --noEmit --pretty false` exited 0. Plan 026 was refreshed for that no-Wrangler retry; no source changes were merged to the main worktree.
- 2026-07-03: plan 026 retry was executed in isolated worktree `/tmp/opencode/ditto-plan-026`, reviewed as DONE on branch `advisor/026-flue-alchemy-build-path`, commit `8d4cd5d`. The implementation keeps Alchemy as the Cloudflare source of truth and does not add `wrangler.jsonc`; `alchemy.run.ts` computes the generated Flue entrypoint from the current checkout root basename. All verification gates pass: `pnpm flue:build`=0 with only the known Durable Object migration warning, generated `dist/ditto_plan_026/index.js` exists in the worktree, `pnpm exec tsc --noEmit --pretty false`=0, `pnpm lint`=0 with only the two known warnings, `pnpm test`=0 (8 files, 62 tests), `git diff --check`=0, and generated artifact status clean. The commit also adds `**/alchemy.run.ts` to `biome.json` so the existing lefthook pre-commit can process the staged file normally; no hook bypass was used.
- 2026-07-03: plan 028 was executed in isolated worktree `/tmp/opencode/ditto-plan-028`, reviewed as DONE on branch `advisor/028-project-coordinator-sqlite`, commits `268d199` and `aa84c82`. All verification gates pass: `pnpm exec tsc --noEmit --pretty false`=0, `pnpm lint`=0 with only the two known warnings, `pnpm test`=0 (10 files, 73 tests), and `git diff --check`=0. Scope is clean: only `src/lib/project-coordinator.ts`, `src/lib/project-coordinator-sqlite.ts`, and `src/lib/project-coordinator-sqlite.test.ts` changed. One reviewer-approved deviation was required: the new test imports `project-coordinator.ts`, so it adds the existing `cloudflare:workers` mock seam before asserting the planned missing-module failure and row-transform behavior.
- 2026-07-03: plan 029 was executed in isolated worktree `/tmp/opencode/ditto-plan-029`, reviewed as DONE on branch `advisor/029-r2-snapshot-artifact-layout`, commits `cc209a4`, `e4b34d5`, and `ffda1a2`. All verification gates pass: `pnpm test -- src/lib/r2-layout.test.ts`=0 (13 new tests), `pnpm exec tsc --noEmit --pretty false`=0, `pnpm lint`=0 with only the two known warnings, `pnpm test`=0 (11 files, 86 tests), and `git diff --check`=0. Scope is clean: only `src/lib/r2-layout.ts` and `src/lib/r2-layout.test.ts` changed. One revision round corrected the public API to match the plan: `buildSnapshotManifest` now accepts the written `r2Key`, and `resolveSnapshotPointer` accepts `{ ok: boolean }` rather than a bare boolean.
- 2026-07-03: added Phase 2 plans 030–034 against commit `e915ff1` (master HEAD after plans 025-028 merged; plan 029 is on branch `advisor/029-r2-snapshot-artifact-layout` but not yet merged to master — it is not a Phase 2 dependency). Flue event types, dispatch API (`POST /agents/:name/:id`), stream API (`GET /agents/:name/:id?offset=&live=`), `defineTool`/`valibot` tool schemas, and `createAgent`/`cloudflareSandbox` patterns were verified from the installed `@flue/runtime@1.0.0-beta.1` type declarations (`node_modules/@flue/runtime/dist/types-DU_ZkvZJ.d.mts`) and Context7 docs. The Phase 0 decision doc's session-mapping decision ("external submissions cannot select a session — `SUBMISSION_SESSION_NAME = "default"`") was confirmed from the installed adapter types (`node_modules/@flue/runtime/dist/adapter.d.mts`), so Phase 2 carries the product session ID in D1 and uses the default Flue session; named session opening is deferred. No source files were changed by this planning pass — only `plans/` markdown.

## Findings considered and rejected / deferred

- Lockfile churn from the uncommitted dependency update was intentionally ignored per maintainer instruction on 2026-06-25.
- Escaped GitHub App private-key normalization: rejected per maintainer instruction on 2026-06-25; current raw-key behavior is intentional.
- Exact xterm or raw Pi terminal mirroring is intentionally deferred from plan 017. The maintainer chose the structured Pi RPC event stream inside Ditto's existing chat UI for the foundation path.
- 2026-07-02 deferred findings 6–11: provisioning-error redaction, real env validation, production devtools gating, high audit advisories, read-side sandbox repair, and stale docs/manifest/old plan statuses are recorded in `plans/deferred-findings-2026-07-02.md` for later planning.
- Plans 021–024: rejected for future execution because they target the superseded Pi runner/broker architecture. Plan 025 replaces them as the next architecture step.
