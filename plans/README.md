# Implementation Plans

Generated by the improve skill. Execute in order unless dependencies say otherwise.
Each executor: read the plan fully before starting, honor STOP conditions, and update
your row when done.

## Context (for humans)

Ditto already has:

- `@cloudflare/sandbox` Container + R2 `createBackup` / `restoreBackup`
- Project wake/hydrate via `ensureProjectSandbox`
- D1 tables: `projects`, `workspace_sessions` (with unused `branchName` /
  `baseCommitSha` / `workspacePath`), `messages`
- PI harness in the sandbox image; client streams `POST /api/agent/stream` (SSE)
- GitHub App installation tokens for **clone only**; remote scrubbed after clone
- Pure helpers in `src/lib/github-export.ts` (no production callers yet)
- Composer hardcodes branch label `"master"`

## Plans 001–004 (agent harness) — DONE

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Bake PI AI harness into sandbox image | P1 | M | — | DONE |
| 002  | Worker agent run + SSE stream + post-run backup | P1 | L | 001 | DONE |
| 003  | Stream agent events in chat UI + architecture docs | P1 | M | 002 | DONE |
| 004  | Remove Effect anti-patterns + deslop chat UI | P1 | M | — | DONE |

## Plans 005–007 (session git + GitHub export) — TODO

Planned at commit `6ac3b74` (2026-07-09) after research + product interview.

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 005  | Session worktrees + branch lifecycle | P1 | L | — (post 001–003) | DONE |
| 006  | Worker commit / push / open PR + UI | P1 | L | 005 | DONE |
| 007  | Agent chat tools for push/PR via Worker JWT callback | P2 | M | 005, 006 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (reason) | REJECTED (rationale)

## Dependency notes

- **006 requires 005** — git ops use `workspaceSessions.workspacePath` (worktree).
- **007 requires 006** — agent tools call the same `session-git` functions as the UI.
- **Merge** is explicitly out of 006/007 (deferred product decision).

## Product decisions (locked 2026-07-09)

| Topic | Decision |
|-------|----------|
| Concurrency isolation | **Git worktrees** per session under `/workspace/.ditto/worktrees/<sessionId>` |
| Dependencies cost | **Symlink** `/workspace/node_modules` and `.env` into each worktree (no reinstall) |
| Branch lifecycle | Create branch + worktree **on first agent message** (lazy) |
| Branch naming | `ditto/session-<shortSessionId>` |
| Who commits | User via UI; agent may **local** `git commit` via bash |
| Push / open PR | **Worker only**, GitHub **installation** token; never in agent env |
| Chat-driven push/PR | Custom PI tools → Worker JWT callback (plan 007) |
| Merge in v1 | **No** |
| Sandbox SDK vs harness for git writes | SDK has **clone only** (`gitCheckout`); all other git is `sandbox.exec` from Worker (or agent bash for local-only) |

## Research summary (why not “branch only”)

One project = one sandbox FS. Checking out a second session branch moves the
**same** working tree; agent A then edits agent B’s branch. Branch metadata
alone does not isolate concurrent agents. Worktrees give each session its own
tree while sharing git objects and (via symlink) `node_modules`.

## PI / Sandbox facts

- PI tools: `read | bash | edit | write | grep | find | ls` — **no git tool**
- PI supports `customTools` on `createAgentSession` (for plan 007)
- `@cloudflare/sandbox@0.12.1` git API: **`gitCheckout` only**; commit/push via `exec`
- Cloudflare docs: [Work with Git](https://developers.cloudflare.com/sandbox/guides/git-workflows/),
  [Proxy requests](https://developers.cloudflare.com/sandbox/guides/proxy-requests/) (pattern for keeping secrets out of sandbox)

## Findings considered and rejected

- **Branch-only without worktrees for concurrency**: rejected — does not isolate FS
- **Inject installation token into agent shell for `git push`**: rejected — credential exposure
- **Sandbox-per-session**: rejected for now — breaks 1-project-1-sandbox, expensive
- **Merge in v1**: deferred by product choice
- **Mount R2 over `/workspace`**: still rejected (see 001–003 notes)
- **App-layer mutex instead of worktrees**: not chosen; worktrees preferred for true concurrent sessions

## Recommended execution order

1. `005-session-worktrees-and-branches.md`
2. `006-worker-git-export-and-ui.md`
3. `007-agent-worker-git-tools.md`
