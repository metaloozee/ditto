# Implementation Plans

Generated by the improve skill on 2026-06-19. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Complete the GitHub OAuth popup flow | P1 | M | — | DONE |
| 002 | Add GitHub loading timeout and fix broken loading copy | P1 | S | 001 | DONE |
| 003 | Extract the GitHub repository loader from the dialog | P2 | M | 001, 002 | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale — finding fixed independently or approach abandoned)

## Dependency notes

- 002 depends on 001 because timeout/listener cleanup should be added to the final message-aware popup waiter, not the temporary close-only waiter.
- 003 depends on 001 and 002 because extraction should preserve the final callback URL, popup completion message, and timeout behavior rather than freezing the current buggy OAuth flow into a shared module.

## Verification baseline

Use these commands when executing plans:

- `pnpm exec tsc --noEmit` — passed during advisor recon.
- `pnpm lint` — passed during advisor recon with two known pre-existing warnings in `src/components/ui/sidebar.tsx` about direct `document.cookie` assignment.
- `pnpm check` — known to fail before these plans because several `src/components/ui/*` files have pre-existing Biome import-order issues. Do not treat those as caused by these plans unless the touched files introduce new check failures.
- `pnpm test` — available; repository currently has no source tests discovered during planning.

## Findings covered

- Popup OAuth flow can hang forever because the dialog waits only for popup closure while the Better Auth callback renders the app in the popup.
- GitHub loading state has no timeout/cancel recovery path.
- GitHub loading copy contains a replacement character: `Loading repositories�`.
- GitHub OAuth/token/API logic is embedded in a large dialog component and should be extracted after behavior is fixed.

## Findings considered and rejected

- Main app reload after GitHub selection: rejected as an active finding for the current diff because `disableRedirect: true` is already present in `src/components/new-project-dialog.tsx`. The remaining issue is popup completion, not main-window redirect.
